// Kept as a harmless compatibility command: never overwrite the live portal
// shell with dated announcements or download another static snapshot.
console.log("The Gradelink portal now loads live public announcements. Use npm run build; no snapshot generation is needed.");
